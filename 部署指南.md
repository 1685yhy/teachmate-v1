# Vercel 部署指南

## ✅ 代码已推送到 GitHub
- 仓库地址：https://github.com/1685yhy/teachmate-v1.git
- 最新提交：已包含百度语音识别 API 和所有优化功能

## 🚀 Vercel 部署步骤

### 步骤 1：登录 Vercel
1. 访问 [Vercel](https://vercel.com/)
2. 使用 GitHub 账号登录（推荐）或邮箱注册

### 步骤 2：导入项目
1. 点击 **"Add New"** → **"Project"**
2. 在 **Import Git Repository** 中搜索 `teachmate-v1` 或直接输入仓库地址
3. 选择仓库：`1685yhy/teachmate-v1`
4. 点击 **"Import"**

### 步骤 3：配置项目
1. **Framework Preset**: Next.js（自动识别）
2. **Root Directory**: `./`（默认）
3. **Build Command**: `npm run build`（默认）
4. **Output Directory**: `.next`（默认）
5. **Install Command**: `npm install`（默认）

### 步骤 4：配置环境变量（重要！）

点击 **"Environment Variables"** 添加以下变量：

#### 必需的环境变量：

**1. DeepSeek API（用于教案生成和课堂分析）**
```
OPENAI_API_KEY = sk-85a2f65933804eec97edf9f539922794
OPENAI_BASE_URL = https://api.deepseek.com
```

**2. 百度语音识别 API（免费方案，推荐）**
```
BAIDU_SPEECH_API_KEY = AfVytoeS8uldH12Orfx6UbOU
BAIDU_SPEECH_SECRET_KEY = 0zgwQuiVupNe4CF2Uzc1cTihaVr7jvvM
BAIDU_SPEECH_APP_ID = 7357988
```

**3. OpenAI Whisper API（可选，付费方案）**
如果需要使用 Whisper，添加：
```
OPENAI_WHISPER_API_KEY = sk-your-openai-api-key-here
```

#### 环境变量配置说明：
- 每个变量都要分别添加
- **Environment** 选择：`Production`, `Preview`, `Development`（全选）
- 添加完成后点击 **"Deploy"**

### 步骤 5：部署
1. 点击 **"Deploy"** 按钮
2. 等待构建完成（通常需要 2-5 分钟）
3. 部署成功后，Vercel 会自动分配一个域名，例如：`teachmate-v1.vercel.app`

### 步骤 6：验证部署
1. 访问部署的网站
2. 测试功能：
   - ✅ 上传音频文件，测试百度语音识别
   - ✅ 生成教案
   - ✅ 深度分析功能

## 📝 注意事项

### 环境变量安全
- ✅ `.env.local` 文件已添加到 `.gitignore`，不会被提交到 Git
- ✅ 所有敏感信息都通过 Vercel 环境变量配置
- ⚠️ 不要在代码中硬编码 API Key

### 免费额度
- **百度语音识别**：每天 5 万次免费调用
- **Vercel**：免费套餐足够个人项目使用

### 更新代码
以后更新代码时，只需：
```bash
git add .
git commit -m "更新说明"
git push origin main
```
Vercel 会自动检测到更新并重新部署。

## 🔗 相关链接

- GitHub 仓库：https://github.com/1685yhy/teachmate-v1
- Vercel 文档：https://vercel.com/docs
- 百度语音识别文档：https://ai.baidu.com/ai-doc/SPEECH/Vk38lxily

